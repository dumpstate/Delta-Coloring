#ifndef DELTACOLORING_MERGE_COMPONENTS
#define DELTACOLORING_MERGE_COMPONENTS

namespace dColoring {
	template<typename Graph, typename Components, typename ColorsMap>
	int merge_components(Graph& graph, Components& components, ColorsMap& col_map) {
		LOG1("merging");
		return 0;
	}
}

#endif //DELTACOLORING_MERGE_COMPONENTS